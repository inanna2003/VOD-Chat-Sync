# プライバシーポリシー — VOD チャット同期

_最終更新: 2025年_

本拡張機能はブラウザ内で完結するクライアントとして設計されており、個人情報の
収集・送信・販売・共有・処理を一切行いません。

## 1. 本拡張機能が保存するデータ

本拡張機能は、ブラウザ標準の `chrome.storage` API を使い、以下のデータを
**お使いの端末上にのみ**保存します。

- ユーザー設定（チャット不透明度、チャット幅、エモートサイズ、ヘッダ非表示／
  ステータスバー非表示／クリックでシークなどのトグル、有効化スイッチ、PiP
  モード）。
- サードパーティ API（§3 参照）から取得した公開エモート・バッジメタデータの
  短期キャッシュ。チャンネル ID をキーとし、TTL は 24 時間（失敗時は 1 時間）。
  内容は画像 URL とエモートコードのみで、個人情報は含みません。
- 「診断情報をコピー」機能用の、直近 50 件のエラーログを保持するメモリ上の
  リングバッファ。自動アップロードは行いません。「診断情報をコピー」を実行
  した場合、内容はクリップボードにのみ置かれます。バッファに入る前に、
  OAuth / Bearer トークンや `access_token=` クエリ文字列は伏字に置換されます。

ブラウザの同期機能が有効で、データ量が `storage.sync` のクォータ内であれば、
ご自身の他端末との間で設定がブラウザベンダー（Google / Microsoft / Brave）に
よって同期されることがあります。これはブラウザの機能であり、本拡張機能が
提供するものではありません。本拡張機能はブラウザが用意するストレージスロット
に書き込んでいるだけです。

ブラウザストレージサンドボックス外への書き込みは一切行わず、Cookie や
IndexedDB も使用せず、現在開いている Twitch タブの URL および `<video>` 要素
以外のデータは読み取りません。

## 2. 本拡張機能が**収集しないもの**

- 氏名・メールアドレス・アカウント識別子
- Twitch のログイン情報（本拡張機能は Twitch にログインしません）
- 閲覧履歴
- アナリティクス・テレメトリ・クラッシュレポート・利用統計の類
- A/B テスト・広告・フィンガープリンティング
- キーストローク・マウス座標・本ポリシー §3 で述べる VOD チャット再生データ
  以外のページ内容

## 3. 通信先のサードパーティサービス

本拡張機能はチャットリプレイおよびそのエモート／バッジを描画する目的のみで、
以下のホストに**未認証・匿名**の HTTP GET / POST リクエストを送信します。

| ホスト                         | 用途                                          | 認証あり |
| ----------------------------- | --------------------------------------------- | ------- |
| `gql.twitch.tv`               | VOD チャットリプレイの取得 (GraphQL)            | なし    |
| `static-cdn.jtvnw.net`        | Twitch 公式エモート／バッジ画像のロード         | なし    |
| `api.betterttv.net`           | BetterTTV エモートメタデータ取得               | なし    |
| `cdn.betterttv.net`           | BetterTTV エモート画像のロード                  | なし    |
| `api.frankerfacez.com`        | FrankerFaceZ エモートメタデータ取得            | なし    |
| `7tv.io`                      | 7TV エモートメタデータおよび画像取得            | なし    |

これらの通信にはブラウザ標準のヘッダ（User-Agent、Accept など）と、現在
視聴しているチャンネル ID または動画 ID が含まれます。アカウント認証情報は
含まれません。これらサービス側のログ取り扱いは、それぞれのプライバシー
ポリシーに従います。本拡張機能の作者はこれらのサービスといかなる特別な関係も
なく、それらサーバーのログを閲覧することもできません。

本拡張機能の作者は独自のサーバーを運用しておらず、自前のバックエンドは
存在しません。

## 4. 権限とその目的

| 権限                              | 目的                                                                  |
| -------------------------------- | -------------------------------------------------------------------- |
| `storage`                        | §1 で述べる設定の保存                                                 |
| `tabs`                           | ツールバーポップアップの「今すぐ PiP で開く」コマンドをアクティブな Twitch タブへ送信する |
| `https://www.twitch.tv/*`        | VOD ページの検出と UI 埋め込み                                         |
| `https://gql.twitch.tv/*`        | VOD チャットリプレイ取得（未認証 GraphQL）                            |
| `https://static-cdn.jtvnw.net/*` | チャットメッセージ内の Twitch 公式エモート／バッジ画像のロード         |
| `https://api.betterttv.net/*` および `https://cdn.betterttv.net/*` | チャット内 BetterTTV エモートのロード |
| `https://api.frankerfacez.com/*` | チャット内 FrankerFaceZ エモートのロード                              |
| `https://7tv.io/*`               | チャット内 7TV エモートのロード                                        |

`webRequest`、`<all_urls>`、`cookies`、`history`、`bookmarks` などの広範な権限
は要求しません（必要ありません）。

## 5. 子どもの利用について

本拡張機能は子どもを対象として設計されていません。そもそも誰からも情報を
収集していないため、子どもからの情報も収集していません。

## 6. ポリシーの変更

本ポリシーに重要な変更がある場合、このファイル内で反映し「最終更新」日を更新
します。重要な変更はリリースノート／`CHANGELOG.md` にも記載します。

## 7. 連絡先

本ポリシーに関するご質問は、GitHub リポジトリの Issue にて受け付けます。
<https://github.com/YOUR_GITHUB_USER/vod-chat-sync/issues>

（`YOUR_GITHUB_USER` の部分はリポジトリ作成後に実際の所有者名に置き換えて
ください。）
